"""Per-process microphysics modules.

One module per Fortran subroutine being ported. Each exposes a single
pure-functional entry point per ADR-009. Stubs raise NotImplementedError
until populated (see ``docs/PLANS.md`` Milestone 3).
"""
