# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# Editors
.vscode/
.idea/
*.swp

# OS
.DS_Store

# Local Fortran build artifacts (if Fortran is built outside mam4-original-src-code)
*.o
*.mod
*.exe

# Transient build/run directories created when building the vendored Fortran
# reference in place. The committed source under mam4-original-src-code/ stays
# pristine; build artifacts live next to it but are not tracked.
mam4-original-src-code/build/
mam4-original-src-code/run/

# Local validation artifacts cached by scripts/diffrax_24h_validation.py.
# Regenerable from the committed 24h Fortran fixtures + the JAX driver.
scripts/_artifacts/
